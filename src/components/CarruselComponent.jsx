import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CarruselComponent = () => {
	return (
		<View>
			<Text>CarruselComponent</Text>
		</View>
	);
};

export default CarruselComponent;

const styles = StyleSheet.create({});
