import { StyleSheet, Text, View } from "react-native";

const LocationShowComponent = () => {
	return (
		<View style={styles.container}>
			<Text>LocationShowComponent</Text>
		</View>
	);
};

export default LocationShowComponent;

const styles = StyleSheet.create({
	container: {
		backgroundColor: "#fff",
	},
});
